

/** \brief Funcion que devuelva la cantidad de pares de un vector
 *
 * \param int [] Es el vector
 * \param int Cantidad de valores dentro del vector
 * \return Cantidad de pares que hay dentro del vector
 *
 */
int contarPares (int[],int);
/** \brief Determina si un caracter esta o no esta dentro del array
 *
 * \param char[] Coleccion de caracteres (vector)
 * \param int    tamaño de la coleccion (cantidad de vectores)
 * \param char  caracter a buscar
 * \return int  "-1 " si no lo encuntra o el "indice" del valor encontrado
 *
 */
 /** va a devolver un ENTERO que es el lugar del subindice en el que se encuentra esa letra**/
int buscarChar(char[],int,char);
