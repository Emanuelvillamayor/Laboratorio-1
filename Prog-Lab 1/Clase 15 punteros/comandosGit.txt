git clone repo
//cd al directorio del repo ej: 
cd PunterEnC-


    git status 
    //si hay rojo tengo que pones :
    git add . 
    git commit -m "mensaje de porque se guarda"
    ... los 4 anteriores es un bucle que se realiza varias veces 

    git push origin master -f
  